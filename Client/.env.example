# Variables de entorno del cliente (Vite).
# Copia este archivo como ".env" o ".env.local" (ambos ignorados por git).
# Todas las variables públicas del frontend deben empezar con VITE_.

# URL base del API. Sin barra al final.
VITE_API_URL=http://localhost:5286/api
